#!/bin/bash
# AppRun — entry point for the LE Beta Vis AppImage
HERE="$(dirname "$(readlink -f "$0")")"
export LD_LIBRARY_PATH="${HERE}/usr/bin/_internal:${HERE}/usr/bin:${HERE}/usr/lib:${LD_LIBRARY_PATH}"
exec "$HERE/usr/bin/lbnlvis" "$@"
